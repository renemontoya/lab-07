<% if object.errors.any? %>
  <div class="alert alert-danger">
    <h6><%= pluralize(object.errors.count, "error") %>:</h6>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>